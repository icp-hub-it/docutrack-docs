---
sidebar_position: 5
title: retry_user_canister_creation
---
# retry_user_canister_creation

Retries the creation of the user canister for the current user.

**Returns:**

- `RetryUserCanisterCreationResponse` — Indicates the result of the retry.

### Candid Definition

```candid
retry_user_canister_creation : () -> (RetryUserCanisterCreationResponse);
```