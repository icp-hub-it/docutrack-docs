---
sidebar_position: 8
title: request_file
---
# request_file

Creates a new file request for upload.

**Arguments:**

- `path: text` — Upload destination path.

**Returns:**

- `RequestFileResponse` — Contains file alias for upload.

### Candid Definition

```candid
request_file : (text) -> (RequestFileResponse);
```