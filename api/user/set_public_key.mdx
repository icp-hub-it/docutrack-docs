---
sidebar_position: 10
title: set_public_key
---
# set_public_key

Updates the user's public key.

**Arguments:**

- `blob: blob` — The new public key in binary format.

### Candid Definition

```candid
set_public_key : (blob) -> ();
```