---
sidebar_position: 5
title: get_requests
---
# get_requests

Returns a list of file requests made by the user.

**Returns:**

- `vec PublicFileMetadata` — List of file request metadata.

### Candid Definition

```candid
get_requests : () -> (vec PublicFileMetadata) query;
```