---
sidebar_position: 4
title: get_alias_info
---
# get_alias_info

Returns info about a file alias being uploaded.

**Arguments:**

- `alias: text` — The file alias.

**Returns:**

- `Result` — Alias lookup result with file ID and metadata.

### Candid Definition

```candid
get_alias_info : (text) -> (Result) query;
```