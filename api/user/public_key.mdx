---
sidebar_position: 7
title: public_key
---
# public_key

Returns the public key of the user.

**Returns:**

- `blob` — The user's public key in binary format.

### Candid Definition

```candid
public_key : () -> (blob) query;
```